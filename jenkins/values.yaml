controller:
  image:
    tag: "2.528.2-jdk21"

  serviceType: ClusterIP

  resources:
    requests:
      cpu: "200m"
      memory: "512Mi"
    limits:
      cpu: "2"
      memory: "4Gi"

  # 🔴 ABSOLUTE REQUIREMENTS FOR MICROK8S
  JCasC:
    enabled: false

  sidecars:
    configAutoReload:
      enabled: false

persistence:
  enabled: true
  size: 8Gi
  storageClass: microk8s-hostpath
